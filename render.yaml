services:
  - type: web
    name: my-web-app
    env: node
    buildCommand: "npm install"
    startCommand: "npm start"
    branch: main
    plan: free
    envVars:
      - key: DATABASE_URL
        value: "your-database-url"
    autoDeploy: true
